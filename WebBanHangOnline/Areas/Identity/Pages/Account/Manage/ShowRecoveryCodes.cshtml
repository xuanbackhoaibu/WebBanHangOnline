@page
@model ShowRecoveryCodesModel
@{
    ViewData["Title"] = "Mã khôi phục";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning">
    <strong>⚠ Lưu ý quan trọng</strong>
    <ul class="mb-0 mt-2">
        <li>Hãy lưu các mã này ở nơi an toàn.</li>
        <li>Mỗi mã chỉ được sử dụng <strong>một lần</strong>.</li>
        <li>Nếu mất cả thiết bị và mã khôi phục, bạn sẽ không thể đăng nhập.</li>
    </ul>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body">
                <h6 class="card-title mb-3">Danh sách mã khôi phục</h6>

                <div class="row">
                    @for (int i = 0; i < Model.RecoveryCodes.Length; i++)
                    {
                        <div class="col-6 mb-2">
                            <code class="d-block p-2 border rounded bg-light text-center">
                                @Model.RecoveryCodes[i]
                            </code>
                        </div>
                    }
                </div>

                <div class="mt-3 text-muted">
                    Gợi ý: bạn nên chụp màn hình hoặc lưu các mã này vào trình quản lý mật khẩu.
                </div>
            </div>
        </div>
    </div>
</div>