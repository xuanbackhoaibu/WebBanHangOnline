@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Đặt lại khóa xác thực";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning">
    <strong>⚠ Cảnh báo bảo mật</strong>
    <ul class="mb-0 mt-2">
        <li>Ứng dụng xác thực (Google Authenticator, Microsoft Authenticator, …) sẽ ngừng hoạt động.</li>
        <li>2FA sẽ bị tắt cho đến khi bạn cấu hình lại.</li>
        <li>Nếu không cấu hình lại, bạn có thể <strong>mất quyền truy cập tài khoản</strong>.</li>
    </ul>
</div>

<form method="post"
      onsubmit="return confirm('Bạn có chắc chắn muốn reset khóa xác thực? Bạn sẽ phải cấu hình lại 2FA!');">
    <button type="submit" class="btn btn-danger">
        🔄 Reset khóa xác thực
    </button>
</form>