@page
@model Disable2faModel
@{
    ViewData["Title"] = "Tắt xác thực hai bước (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<div class="card shadow-sm">
    <div class="card-body">

        <h4 class="mb-3 text-danger">
            @ViewData["Title"]
        </h4>

        <div class="alert alert-warning" role="alert">
            <h6 class="fw-bold mb-2">
                ⚠ Cảnh báo bảo mật
            </h6>

            <p class="mb-2">
                Việc tắt xác thực hai bước sẽ <strong>làm giảm mức độ bảo mật</strong>
                của tài khoản.
            </p>

            <p class="mb-0">
                Thao tác này <strong>chỉ tắt 2FA</strong>, không thay đổi khóa trong ứng dụng xác thực.
                Nếu bạn muốn tạo khóa mới, hãy
                <a asp-page="./ResetAuthenticator" class="fw-bold">
                    đặt lại ứng dụng xác thực
                </a>.
            </p>
        </div>

        <form method="post" class="mt-3">
            <button type="submit"
                    class="btn btn-danger">
                Tắt xác thực hai bước
            </button>

            <a asp-page="./TwoFactorAuthentication"
               class="btn btn-outline-secondary ms-2">
                Hủy bỏ
            </a>
        </form>

    </div>
</div>