@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "Thiết lập ứng dụng xác thực (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />

<div class="card shadow-sm">
    <div class="card-body">

        <h4 class="mb-3">
            @ViewData["Title"]
        </h4>

        <p class="text-muted">
            Để tăng cường bảo mật cho tài khoản, vui lòng thực hiện theo các bước sau:
        </p>

        <ol class="list-group list-group-numbered mb-4">

            <!-- STEP 1 -->
            <li class="list-group-item">
                <strong>Cài đặt ứng dụng xác thực</strong>
                <p class="mb-1">
                    Tải một trong các ứng dụng sau:
                </p>
                <ul class="mb-0">
                    <li>
                        Microsoft Authenticator:
                        <a href="https://go.microsoft.com/fwlink/?Linkid=825072">Android</a>,
                        <a href="https://go.microsoft.com/fwlink/?Linkid=825073">iOS</a>
                    </li>
                    <li>
                        Google Authenticator:
                        <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2">Android</a>,
                        <a href="https://apps.apple.com/app/google-authenticator/id388497605">iOS</a>
                    </li>
                </ul>
            </li>

            <!-- STEP 2 -->
            <li class="list-group-item">
                <strong>Quét mã QR hoặc nhập khóa thủ công</strong>

                <p class="mb-2">
                    Quét mã QR bên dưới hoặc nhập khóa sau vào ứng dụng xác thực:
                </p>

                <div class="mb-2">
                    <kbd class="fs-6">@Model.SharedKey</kbd>
                </div>

                <div class="alert alert-info py-2">
                    <small>
                        Phân biệt chữ hoa – chữ thường và khoảng trắng không quan trọng.
                    </small>
                </div>

                <div id="qrCode" class="mb-2"></div>
                <div id="qrCodeData"
                     data-url="@Model.AuthenticatorUri">
                </div>
            </li>

            <!-- STEP 3 -->
            <li class="list-group-item">
                <strong>Xác nhận mã bảo mật</strong>

                <p class="mb-2">
                    Nhập mã gồm 6 chữ số được tạo bởi ứng dụng xác thực:
                </p>

                <form method="post" class="col-md-6">
                    <div class="form-floating mb-3">
                        <input asp-for="Input.Code"
                               class="form-control"
                               autocomplete="off"
                               placeholder="123456" />
                        <label asp-for="Input.Code">
                            Mã xác thực
                        </label>
                        <span asp-validation-for="Input.Code"
                              class="text-danger"></span>
                    </div>

                    <button type="submit"
                            class="btn btn-primary w-100">
                        Xác nhận &amp; kích hoạt
                    </button>

                    <div asp-validation-summary="ModelOnly"
                         class="text-danger mt-2"
                         role="alert">
                    </div>
                </form>
            </li>

        </ol>

        <a asp-page="./TwoFactorAuthentication"
           class="btn btn-outline-secondary">
            Quay lại cài đặt bảo mật
        </a>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
