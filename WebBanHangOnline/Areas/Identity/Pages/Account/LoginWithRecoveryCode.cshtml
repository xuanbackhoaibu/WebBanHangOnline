@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Đăng nhập bằng mã khôi phục";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">

            <div class="card shadow-sm">
                <div class="card-body p-4">

                    <h3 class="text-center mb-3">
                        🛟 Mã khôi phục
                    </h3>

                    <p class="text-muted text-center mb-4">
                        Bạn đang đăng nhập bằng <strong>recovery code</strong>.
                        <br />
                        Phiên đăng nhập này sẽ <b>không được ghi nhớ</b>
                        cho đến khi bạn xác thực lại bằng ứng dụng 2FA
                        hoặc tắt 2FA.
                    </p>

                    <form method="post">
                        <div asp-validation-summary="ModelOnly"
                             class="text-danger mb-3"
                             role="alert">
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Input.RecoveryCode"
                                   class="form-control"
                                   autocomplete="off"
                                   placeholder="Nhập mã khôi phục" />
                            <label asp-for="Input.RecoveryCode">
                                Mã khôi phục
                            </label>
                            <span asp-validation-for="Input.RecoveryCode"
                                  class="text-danger">
                            </span>
                        </div>

                        <button type="submit"
                                class="w-100 btn btn-lg btn-primary">
                            Đăng nhập
                        </button>
                    </form>

                    <hr class="my-4" />

                    <p class="text-center mb-0">
                        Đã có lại ứng dụng xác thực?
                        <br />
                        <a asp-page="./Login">
                            ← Quay lại đăng nhập thường
                        </a>
                    </p>

                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
