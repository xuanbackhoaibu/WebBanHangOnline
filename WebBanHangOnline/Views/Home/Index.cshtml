@model List<WebBanHangOnline.Models.Product>
@{
    ViewData["Title"] = WebBanHangOnline.Helpers.CatalogTextHelper.T("Trang chủ", "Home");
    string T(string vi, string en) => WebBanHangOnline.Helpers.CatalogTextHelper.T(vi, en);
    string D(string? value) => WebBanHangOnline.Helpers.CatalogTextHelper.Display(value);
}

@section Styles {
    <link rel="stylesheet" href="~/css/pages/home.css" asp-append-version="true" />
}

<div class="home-page">

<!-- ================= HERO ================= -->
<section class="hero container">
    <div class="hero-content">
        <small class="hero-kicker">XUANBAC FASHION</small>
        <h1>@T("Bộ sưu tập mới cho phong cách mỗi ngày", "New collections for everyday style")</h1>
        <p class="hero-copy">
            @T("Thời trang nam, nữ và trẻ em với trải nghiệm mua sắm nhanh, rõ ràng và dễ lựa chọn.",
                "Fashion for men, women and kids with a fast, clear and easy shopping experience.")
        </p>
        <div class="hero-proof">
            <span><i class="bi bi-lightning-charge-fill"></i> Flash sale</span>
            <span><i class="bi bi-box-seam"></i> @T("Có sẵn tồn kho", "In stock")</span>
            <span><i class="bi bi-credit-card"></i> @T("Nhiều thanh toán", "Multiple payments")</span>
        </div>

        <div class="hero-actions">
            <a asp-controller="Product"
               asp-action="Index"
               class="btn btn-primary btn-lg rounded-pill px-4 hero-btn">
                @T("Mua sắm ngay", "Shop now")
            </a>
            <a href="#women"
               class="btn btn-outline-danger btn-lg rounded-pill px-4 hero-btn">
                @T("Khám phá thời trang nữ", "Explore women's fashion")
            </a>
        </div>
    </div>
    <img src="/images/l.jpg" class="hero-img" alt="Bộ sưu tập thời trang XuanBac" loading="lazy" />
</section>

<!-- ================= WOMEN ================= -->
<section id="women" class="section container">
    <div class="row align-items-center">
        <div class="col-lg-5">
            <img src="/images/women-banner.jpg" class="img-fluid">
        </div>
        <div class="col-lg-7">
            <p class="section-eyebrow">For women</p>
            <h4 class="section-title">@T("Thời trang nữ", "Women's fashion")</h4>
            <div class="row g-4">
                @foreach (var p in Model
                              .Where(x => x.Category != null 
                                          && x.Category.Name == "Đồ Nữ"
                                          && x.IsActive)
                              .Take(6))
                {
                    <div class="col-6 col-md-4">
                        <div class="product-card">
                            <span class="badge hot">HOT</span>
                            <a asp-controller="Product"
                               asp-action="Details"
                               asp-route-id="@p.ProductId"
                               asp-route-slug="@p.Slug">
                                <img src="@p.Thumbnail" />
                            </a>
                            <h6>@D(p.Name)</h6>
                            <div class="price">@p.Price.ToString("N0") VND</div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</section>


<!-- ================= SALE ================= -->
<section class="sale-box">
    <div class="sale-left">
        <img src="/images/d.jpg">
        <p>@T("Balo mini", "Mini backpack")</p>
    </div>
    <div class="sale-center">
        <h3>@T("Khuyến mãi giới hạn", "Limited promotion")</h3>
        <h2>50% OFF</h2>
    </div>
    <div class="sale-right">
        <img src="/images/c.jpg">
    </div>
</section>

<!-- ================= MEN ================= -->
<section class="section container">
    <div class="row align-items-center">
        <div class="col-lg-7">
            <p class="section-eyebrow">For men</p>
            <h4 class="section-title">@T("Thời trang nam", "Men's fashion")</h4>
            <div class="row g-4">
                @foreach (var p in Model
                              .Where(x => x.Category != null 
                                          && x.Category.Name == "Đồ Nam"
                                          && x.IsActive)
                              .Take(6))
                {
                    <div class="col-6 col-md-4">
                        <div class="product-card">
                            <span class="badge sale">SALE</span>
                            <a asp-controller="Product"
                               asp-action="Details"
                               asp-route-id="@p.ProductId"
                               asp-route-slug="@p.Slug">
                                <img src="@p.Thumbnail" />
                            </a>
                            <h6>@D(p.Name)</h6>
                            <div class="price">@p.Price.ToString("N0") VND</div>
                        </div>
                    </div>
                }
            </div>
        </div>
        <div class="col-lg-5">
            <img src="/images/men-banner.jpg" class="img-fluid">
        </div>
    </div>
</section>

<!-- ================= BOYS ================= -->
<section class="section container">
    <div class="row align-items-center">
        <div class="col-lg-5">
            <img src="/images/boys-banner.jpg" class="img-fluid">
        </div>
        <div class="col-lg-7">
            <p class="section-eyebrow">For boys</p>
            <h4 class="section-title">@T("Thời trang bé trai", "Boys' fashion")</h4>
            <div class="row g-4">
                @foreach (var p in Model
                              .Where(x => x.Category != null 
                                          && x.Category.Name == "Bé Trai"
                                          && x.IsActive)
                              .Take(6))
                {
                    <div class="col-6 col-md-4">
                        <div class="product-card">
                            <span class="badge hot">HOT</span>
                            <a asp-controller="Product"
                               asp-action="Details"
                               asp-route-id="@p.ProductId"
                               asp-route-slug="@p.Slug">
                                <img src="@p.Thumbnail" />
                            </a>
                            <h6>@D(p.Name)</h6>
                            <div class="price">@p.Price.ToString("N0") VND</div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</section>

<!-- ================= GIRLS ================= -->
<section class="section container">
    <div class="row align-items-center">
        <div class="col-lg-7">
            <p class="section-eyebrow">For girls</p>
            <h4 class="section-title">@T("Thời trang bé gái", "Girls' fashion")</h4>
            <div class="row g-4">
                @foreach (var p in Model
                              .Where(x => x.Category != null 
                                          && x.Category.Name == "Bé Gái"
                                          && x.IsActive)
                              .Take(6))
                {
                    <div class="col-6 col-md-4">
                        <div class="product-card">
                            <span class="badge sale">SALE</span>
                            <a asp-controller="Product"
                               asp-action="Details"
                               asp-route-id="@p.ProductId"
                               asp-route-slug="@p.Slug">
                                <img src="@p.Thumbnail" />
                            </a>
                            <h6>@D(p.Name)</h6>
                            <div class="price">@p.Price.ToString("N0") VND</div>
                        </div>
                    </div>
                }
            </div>
        </div>
        <div class="col-lg-5">
            <img src="/images/girls-banner.jpg" class="img-fluid">
        </div>
    </div>
</section>

<!-- ================= TABS ================= -->
<section class="tabs container">
    <div class="tab-head">
        <button class="tab-btn active" data-tab="popular">@T("Phổ biến", "Popular")</button>
        <button class="tab-btn" data-tab="hot">@T("Bán chạy", "Best sellers")</button>
        <button class="tab-btn" data-tab="sale">@T("Khuyến mãi", "On sale")</button>
    </div>

    <!-- ===== PHỔ BIẾN ===== -->
    <div class="tab-body active" id="popular">
        <div class="row g-4">
            @foreach (var p in Model.Take(8))
            {
                <div class="col-6 col-md-3">
                    <div class="product-card">
                        <a asp-controller="Product"
                           asp-action="Details"
                           asp-route-id="@p.ProductId"
                           asp-route-slug="@p.Slug"
                           class="product-link">

                            <img src="@p.Thumbnail" />
                            <h6>@D(p.Name)</h6>
                            <div class="price">@p.Price.ToString("N0") VND</div>

                        </a>
                    </div>
                </div>
            }
        </div>
    </div>

    <!-- ===== BÁN CHẠY ===== -->
    <div class="tab-body" id="hot">
        <div class="row g-4">
            @foreach (var p in Model.Skip(4).Take(8))
            {
                <div class="col-6 col-md-3">
                    <div class="product-card">
                        <a asp-controller="Product"
                           asp-action="Details"
                           asp-route-id="@p.ProductId"
                           asp-route-slug="@p.Slug"
                           class="product-link">

                            <img src="@p.Thumbnail" />
                            <h6>@D(p.Name)</h6>
                            <div class="price">@p.Price.ToString("N0") VND</div>

                        </a>
                    </div>
                </div>
            }
        </div>
    </div>

    <!-- ===== KHUYẾN MÃI ===== -->
    <div class="tab-body" id="sale">
        <div class="row g-4">
            @foreach (var p in Model.Skip(8).Take(8))
            {
                <div class="col-6 col-md-3">
                    <div class="product-card">
                        <a asp-controller="Product"
                           asp-action="Details"
                           asp-route-id="@p.ProductId"
                           asp-route-slug="@p.Slug"
                           class="product-link">

                            <img src="@p.Thumbnail" />
                            <h6>@D(p.Name)</h6>
                            <div class="price">@p.Price.ToString("N0") VND</div>

                        </a>
                    </div>
                </div>
            }
        </div>
    </div>
</section>
<!-- ================= BLOG ================= -->
<section class="blog container">
    <h4 class="text-center mb-4">@T("Xu hướng thời trang", "Fashion trends")</h4>
    <div class="row g-4">
        <div class="col-md-4">
            <div class="blog-card">
                <img src="/images/blog1.jpg" class="img-fluid blog-img" alt="Blog 1" loading="lazy">
                <div class="blog-body">
                    <h6>Beauty life style classic</h6>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="blog-card">
                <img src="/images/blog2.jpg" class="img-fluid blog-img" alt="Blog 2" loading="lazy">
                <div class="blog-body">
                    <h6>The need of life with vip style</h6>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="blog-card">
                <img src="/images/blog3.jpg" class="img-fluid blog-img" alt="Blog 3" loading="lazy">
                <div class="blog-body">
                    <h6>There is someone standing behind you</h6>
                </div>
            </div>
        </div>
    </div>
</section>

</div>

@section Scripts {
<script>
document.querySelectorAll(".tab-btn").forEach(btn => {
    btn.addEventListener("click", () => {
        document.querySelectorAll(".tab-btn").forEach(b => b.classList.remove("active"));
        document.querySelectorAll(".tab-body").forEach(b => b.classList.remove("active"));

        btn.classList.add("active");
        document.getElementById(btn.dataset.tab).classList.add("active");
    });
});
</script>
}
